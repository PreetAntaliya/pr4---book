<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet">

    <title>Book adding</title>

    <style>
        a:hover{
            text-decoration: none !important;
            color: white !important;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="row d-flex justify-content-center">
            <div>
                <form method="post" action="/updateData" enctype="multipart/form-data">

                    <table border="0" cellpadding="10px">
                        <input type="hidden" name="editid" value="<%= single.id %>"
                        <tr>
                            <td>Book Name</td>
                            <td>
                                <input type="text" name="name" value="<%= single.name %>">
                            </td>
                        </tr>
                        <tr>
                            <td>Book Price</td>
                            <td>
                                <input type="text" name="price" value="<%= single.price %>">
                            </td>
                        </tr>
                        <tr>
                            <td>Book Pages</td>
                            <td>
                                <select name="pages">
                                    <% page=["0-50","51-100","101-200","201-300","upto 600"] %>
                                        <option value="">---Select pages--</option>
                                        <% page.map((val)=>{ %>
                                            <option <% if(single.pages == val) { %> selected <% } %> value="<%= val %>">
                                                <%= val %>
                                            </option>
                                            <% }) %>
                                </select>
                            </td>
                        </tr>
                        <tr>
                            <td>Author</td>
                            <td>
                                <input type="text" name="authore" value="<%= single.authore %>"> 
                            </td>
                        </tr>
                        <tr>
                            <td>File Upload</td>
                            <td>
                                <input type="file" name="avatar" >
                                <img src="<%= single.image %> " width="100px">

                            </td>
                        </tr>
                        <tr>
                            <td colspan="2" style="text-align: center;">
                            <input type="submit" value="submit">
                            </td>
                        </tr>
                    </table>
                </form>
                <div>
                    <a href="/">View Available books</a>
                </div>
            </div>
        </div>
    </div>



    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.min.js"></script>
</body>

</html>
